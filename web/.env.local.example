# Copy this file to `.env.local` and replace the placeholders with your real values.
# Do NOT commit your real `.env.local` — it's ignored by default.

NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<your anon key>

# ---- Email ops (admin kill-switches) ----
# Default: safe/disabled.
EMAIL_INBOUND_ENABLED=false
EMAIL_OUTBOUND_ENABLED=false
PORTAL_EMAIL_SEND_ENABLED=false
