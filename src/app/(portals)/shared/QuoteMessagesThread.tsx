"use client";

// Shared messaging UI surface across customer/supplier/admin portals.
// (Re-export to keep existing component stable while providing the requested import path.)
export {
  QuoteMessagesThread,
  type QuoteMessagesThreadProps,
} from "@/app/(portals)/components/QuoteMessagesThread";

